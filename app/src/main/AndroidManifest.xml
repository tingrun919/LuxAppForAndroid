<?xml version="1.0" encoding="utf-8"?>
<manifest package="order.smzs.com.companyorder"
          xmlns:android="http://schemas.android.com/apk/res/android"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Login_Register_Activity"></activity>
        <activity android:name=".Register_Activity"></activity>
        <activity android:name=".UpdatePassWord"></activity>
        <activity android:name=".HotolList_Activity"></activity>

        <activity android:name=".pull.PtrrvListViewMode"></activity>
        <activity android:name=".pull.PtrrvGridViewMode"></activity>
    </application>

</manifest>